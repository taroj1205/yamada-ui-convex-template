import type { ThemeConfig } from "@yamada-ui/react";

export const config: ThemeConfig = {
  breakpoint: { direction: "down", identifier: "@container" },
  initialColorMode: "dark",
  initialThemeScheme: "base",
  var: { prefix: "ui" },
};
